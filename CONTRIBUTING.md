# Contributing

Use work item → `agent/<scope>` branch → focused implementation → QA → PR → required CI → review → merge. Do not push engineering changes directly to canonical `main`.

Every PR must state problem, scope, user impact, checks run, checks not run and release impact. Never label source/static QA as Windows runtime QA.
