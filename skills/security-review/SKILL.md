# Security Review Skill
Review credential handling, shell/process boundaries, update supply chain, installer privileges, artifact signing/hash verification, downgrade controls and secret exposure. Fail closed before executing unverified updates.
