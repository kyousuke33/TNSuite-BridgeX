# Implement Build Skill
Use work item→branch→focused patch→source QA→Windows build evidence→PR. Reuse isolated toolchain, install only missing dependencies, never call static QA a Windows build PASS.
