# Auth Contract
Current desktop transfer authentication follows the bounded client/upstream and Windows OpenSSH mechanisms. Future update checks require no GitHub credential in the client. Portal→RCP publication uses central signed release intent, not client auth.
