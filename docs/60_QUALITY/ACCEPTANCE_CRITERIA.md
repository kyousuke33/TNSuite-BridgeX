# Acceptance Criteria
Never call static/source QA full QA. `WINDOWS_COMPILE_QA=PASS` requires a real successful compile/link/package. GUI runtime requires real Windows evidence. Theme/language acceptance requires first-restart persistence. SFTP acceptance requires actual transfer evidence when in release scope. No web-health surrogate is valid.
