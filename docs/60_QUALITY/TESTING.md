# Testing
Test layers are source/static, compile/API probes, Windows compile/link/package, installer acceptance, GUI runtime, localization/theme runtime, transfer/SFTP acceptance and release artifact verification. Each layer has distinct evidence.
