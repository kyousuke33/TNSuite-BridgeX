# Regression
Retain active Hotfix4–Hotfix16 regression coverage during bootstrap. Historical filenames are technical debt, not proof of obsolescence. Semantic renaming/refactoring is deferred until a later Windows-rebuilt baseline can prove equivalent coverage.
