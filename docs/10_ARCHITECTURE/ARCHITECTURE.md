# Architecture
Canonical source produces a deterministic BridgeX Windows build from SHA-verified upstream FileZilla 3.70.6 plus TNSuite patches, branding, localization, CLI and installer packaging. Generated upstream trees and release binaries are not canonical Git source.
