# Data Flow
Runtime: user → BridgeX UI/local settings → transfer engine → remote FTP/SFTP endpoint.
Planned update: GitHub artifact → RCP verification → Portal signed publish intent → approved artifact/update API → BridgeX manifest check → verified download → installer.
