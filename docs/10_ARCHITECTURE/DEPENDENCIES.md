# Dependencies
Build dependency checks must be deterministic and bounded. Avoid broad environment upgrades when only missing dependencies are required. Self-hosted CI host layout/cleanup/resource policy is owned centrally by RCP contract `tnsuite.ci-runner-host-layout.v1`.
