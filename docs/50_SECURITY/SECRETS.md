# Secrets
Never commit or embed GitHub PATs, signing private keys, private SSH keys or production credentials. Future update verification embeds public verification material only; signing private authority remains external.
