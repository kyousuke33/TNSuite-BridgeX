# Data Retention
Source repo retains source/governance. CI workspace and build outputs are ephemeral per central policy. User AppData persists according to installer product behavior. Future update telemetry needs an explicit retention decision before implementation.
