# Environments
Current meaningful contexts: source/CI, Windows build/QA, optional internal/beta distribution, stable distribution and user device runtime. These are not equivalent to web staging/production hosts.
